name: PlayerInfo
version: '${project.version}'
main: de.erze_stein.Main
api-version: '1.20'
commands:
  playerinfo:
    description: Give Player infos
    permission: playerinfo.use
permissions:
  playerinfo.use:
    description:
    default: true